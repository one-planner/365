<!DOCTYPE html>
<html lang='en'>
  <head>
    <meta charset='utf-8' />
    <script src='https://cdn.jsdelivr.net/npm/fullcalendar@6.1.10/index.global.min.js'></script>
    <style>
      /* Flexbox 초기화 */
      #calendarContainer {
        display: flex;
        flex-wrap: wrap; /* 필요에 따라 wrap 설정 */
        justify-content: space-around; /* 컨테이너 안에서 간격 조절 */
        align-items: flex-start; /* 수직 정렬을 위해 필요한 경우 설정 */
      }

      .calendar {
        flex: 1;
        margin: 10px;
        box-sizing: border-box; /* 요소의 패딩과 테두리를 포함하여 크기 계산 */
      }

      #calendar1 {
        max-width: 300px; /* 최대 너비 설정 */
        height: 100px; /* 최대 높이를 부모 컨테이너에 맞게 설정 */
        overflow: hidden; /* 스크롤 제거 */
      }
      
      .fc-dayGridMonth-view .fc-day {
        height: 20px; /* 원하는 높이로 설정 */
      }
    </style>
    <script>
      document.addEventListener('DOMContentLoaded', function() {
		  
        var calendarEl1 = document.getElementById('calendar1');
        var calendar1 = new FullCalendar.Calendar(calendarEl1, {
          initialView: 'dayGridMonth',
          height: 650,
          dayMaxEvents: 3, // 한 셀에 나타날 최대 이벤트 개수
          dayMaxEventRows: 3 // 한 셀에 나타날 최대 이벤트 행의 개수
        });
        calendar1.render();


        var calendarEl2 = document.getElementById('calendar2');
        var calendar2 = new FullCalendar.Calendar(calendarEl2, {
          initialView: 'dayGridMonth'
        });
        calendar2.render();
      });
    </script>
  </head>
  <body>
    <!-- 달력을 담을 컨테이너 추가 -->
    <div id='calendarContainer'>
      <!-- 첫 번째 달력 (미니 달력) -->
      <div id='calendar1' class='calendar'></div>
      <!-- 두 번째 달력 (크게) -->
      <div id='calendar2' class='calendar'></div>
    </div>
  </body>
</html>
